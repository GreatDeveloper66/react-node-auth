// authMiddleware.js
export default const authenticateUser = (req, res, next) => {
    // Check if user is authenticated (e.g., check if JWT is present and valid)
    // If authenticated, call next()
    // If not, return an unauthorized response
  };
  
  
  